#include "robotconfig.h"
#include <atomic>
#include <cmath>
#include <iostream>

/*

namespace balls {
class Lights : public ryan::TaskWrapper {

public:
    enum class TEAM { Red, Blue, Skills };
    enum class GAMESTATE { Initializing, Disabled, Autonomous, Driver };

private:
    TEAM team;
    GAMESTATE gamestate;
    sylib::Addrled &intakeLED;
    sylib::Addrled &LwingusLED;
    sylib::Addrled &RwingusLED;
    sylib::Addrled &deadassLogoLED;

public:
    Lights(TEAM team, GAMESTATE gamestate, sylib::Addrled &intakeLED, 
    sylib::Addrled &LwingusLED, sylib::Addrled &RwingusLED, sylib::Addrled &deadassLogoLED) 
    {};

    void update( TEAM team, GAMESTATE gamestate );

    void loop() override;

};
}

*/